"""Shared application constants."""

# Courses tagged with this in `courses.tags` are load-test bulk data (`seed_bulk.py`).
# List/reporting APIs exclude them so demos and screenshots stay clean.
BULK_SEED_TAG = "bulk-seed"
